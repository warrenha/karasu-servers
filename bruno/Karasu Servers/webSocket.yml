info:
  name: webSocket
  type: websocket
  seq: 4

websocket:
  url: ws://localhost:3000/ws
  message:
    type: json
    data: |
      { "message": "test" }
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
