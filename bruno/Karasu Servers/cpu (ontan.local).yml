info:
  name: cpu (ontan.local)
  type: http
  seq: 4

http:
  method: GET
  url: http://ontan.local:3000/sysinfo/cpu
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
