info:
  name: webSocket (ontan.local)
  type: websocket
  seq: 12

websocket:
  url: ws://ontan.local:3000/ws
  message:
    type: json
    data: |
      { "message": "test" }
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
